export const C = '&lt;'; //because C looks like '<'
export const D = '&gt;'; //because D looks like '>'
export let Count = 0;

export enum CommentType {
	Leading = 'leading',
	Trailing = 'trailing',
}